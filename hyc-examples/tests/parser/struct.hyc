struct test;

struct another_test;

struct string {
    data: *char;
    len: usize;
    cap: usize;
}

struct vector {
    data: *T;
    len: usize;
    cap: usize = 8;
}

pub petal lexer {
    pub struct Lexer {
        program: &Program = program;
        source: std::string;
        pos: usize = 0;
    }
}
